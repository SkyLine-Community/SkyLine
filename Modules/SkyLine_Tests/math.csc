allow Subtract = function(x, y) {
    return x - y
}

allow Multiply = function(x, y) {
    return x * y
}

allow Divide = function(x, y) {
    return x / y
}

allow Addition = function(x, y) {
    return x + y
}

